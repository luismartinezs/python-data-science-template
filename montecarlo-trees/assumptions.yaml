base_demand: 120        # units / month
base_price:  3          # USD
mu:    [0, 0]             # drift of log-shocks
sigma: [0.3, 0.2]        # volatilities
corr:  [[1,  0.4],       # corr matrix (demand, price)
        [0.4, 1]]
